package isp;

// Создаем интерфейс для любого объекта, который может есть, работник - человек может, как может и теоретически
//другой биологический, но не механический объект
public interface Eater {
    void eat();
}
